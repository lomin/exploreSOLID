apply plugin: 'java'

version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    compile project(':claim')
    compile project(':product')

    testCompile group: 'junit', name: 'junit', version: '4.11'
}